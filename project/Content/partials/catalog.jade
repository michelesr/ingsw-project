h1.page-header Export catalog

p Your catalog includes:
ul
  li {{ productsCount }} products
  li {{ stocksCount }} stocks

a(ng-click='export(authSupplierId)')
  button.btn.btn-primary(type='button')
    span Export catalog (old)

a(ng-click='export2(authSupplierId)')
  button.btn.btn-primary(type='button')
    span Export catalog (new)
